pre-commit:
  commands:
    test:
      run: npm run test

    generate-docs:
      root: /
      run: doxygen && ./docs/doxygen/latex/make.bat

commit-msg:
  commands:
    validate_commit_message:
      run: npx commitlint --edit
